name: Build PS2 Core and Create ISO

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container: ps2dev/ps2dev:latest
    steps:
      - name: Install System Dependencies
        run: |
          apk update
          apk add --no-cache git make build-base cdrkit

      - name: Checkout Base Code
        uses: actions/checkout@v4

      - name: Fix Git and Submodules
        run: |
          git config --global --add safe.directory /__w/fbalpha2012_cps2/fbalpha2012_cps2
          if [ ! -f "libretro-common/file/retro_dirent.c" ]; then
            rm -rf libretro-common
            git clone --depth 1 https://github.com/libretro/libretro-common.git libretro-common
          fi

      - name: Locate Toolchain
        run: |
          # This finds the actual path of the compiler
          find /usr -name "ee-gcc" || echo "Not found in /usr"
          which ee-gcc || echo "Not in PATH"

      - name: Compile
        run: |
          # Use 'command -v' to find the binary path dynamically
          export EE_BIN=$(dirname $(find /usr -name "ee-gcc" | head -n 1))
          export PATH=$EE_BIN:$PATH
          echo "Using compiler at: $EE_BIN"
          
          make -f Makefile.libretro CC=ee-gcc CXX=ee-g++ AR=ee-ar

      - name: Create ISO
        run: |
          mkdir -p iso_root
          # Copy ELF if it exists, otherwise copy the static lib just for the artifact
          [ -f *.elf ] && cp *.elf iso_root/MAIN.ELF || echo "No ELF found"
          
          echo "BOOT2 = cdrom0:\MAIN.ELF;1" > iso_root/SYSTEM.CNF
          echo "VER = 1.00" >> iso_root/SYSTEM.CNF
          echo "VMODE = NTSC" >> iso_root/SYSTEM.CNF
          
          mkisofs -o fba_cps2.iso -R -J -V "FBA_CPS2" iso_root/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: PS2-Build-Output
          path: |
            *.a
            *.elf
            *.iso
